name: Update Stream Links

on:
  schedule:
    - cron: '0 */5 * * *' # S'exécute toutes les 6 heures
  workflow_dispatch: # Permet de le lancer manuellement depuis GitHub

jobs:
  update-links:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install requests

      - name: Run script
        run: python script.py

      - name: Commit and push if changed
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add latest.json
          git diff --quiet && git diff --staged --quiet || (git commit -m "Auto-update stream links [skip ci]" && git push)
